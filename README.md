# DentiSuite Updates

Public release artifacts for DentiSuite auto-update (Windows NSIS installer).

This repository contains **only**:
- `DentiSuite-Setup-*.exe`
- `latest.yml`
- `*.blockmap`

No source code. No secrets. No `.env`.

Consumed by `electron-updater` (provider=github, owner=wellotek, repo=DentiSuite-updates).
